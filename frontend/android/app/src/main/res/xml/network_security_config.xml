<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext HTTP globally (needed for WiFi OTA sync over LAN) -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
